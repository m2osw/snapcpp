#!/bin/sh -e

# Source debconf library.
. /usr/share/debconf/confmodule


if [ "$1" = "purge" ]
then
    # delete the snapdbproxy logs
    rm -f /var/log/snapwebsites/snapdbproxy.log*
    rm -f /var/log/snapwebsites/secure/snapdbproxy.log*
fi

#DEBHELPER#

# vim: ts=4 sw=4 et nocindent
