#!/bin/sh -e

# Source debconf library.
. /usr/share/debconf/confmodule

PACKAGENAME=snapbackend
if systemctl is-active ${PACKAGENAME}
then
    systemctl stop ${PACKAGENAME}
fi

#DEBHELPER#

# vim: ts=4 sw=4 et nocindent
