#!/usr/bin/make -f
# -*- makefile -*-
# Sample debian/rules that uses debhelper.
# This file was originally written by Joey Hess and Craig Small.
# As a special exception, when this file is copied by dh-make into a
# dh-make output file, you may use that output file without restriction.
# This special exception was added by Craig Small in version 0.37 of dh-make.

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ --with systemd --parallel

# Add the 's' flag to the daemon to make sure we can run as root
#
# TODO: since this is now a daemon all on its own, we can decide how
#       to start it in the .service file and this should not be required
#       anymore...
#
override_dh_fixperms:
	dh_fixperms
	chmod u+s debian/snapmanagerdaemon/usr/bin/snapmanagerdaemon

