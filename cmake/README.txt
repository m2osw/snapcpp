License: LICENSE.txt

Installation instructions: INSTALL.txt

Documentation: http://snapwebsites.org/project/snapCMakeModules

These are the modules required for cmake to run for many of the Snap C++ projects.

NOTE: we need to add this to the build to detect versions in the debian/changelog file:
	dpkg-parsechangelog --show-field Version

