#!/bin/sh
set -e
if test "$1" = "-d"
then
	. dev/version
	rm -f ../BUILD/csspp/doc/csspp-doc-${VERSION}.tar.gz
	make -C ../BUILD/csspp/ csspp_Documentation
elif test "$1" = "-t"
then
	make -C ../BUILD/csspp/
	shift
	TEST="$1"
	shift
	if test -n "$TEST"
	then
		echo run with \"$TEST\"
	fi
	../BUILD/csspp/tests/csspp_tests "$TEST" $*
elif test "$1" = "-i"
then
	make -C ../BUILD/csspp install
else
	make -C ../BUILD/csspp
fi
